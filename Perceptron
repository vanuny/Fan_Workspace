#by python 3


from functools import reduce

class Perceptron(object):
  def __init__(self,input_num,activatior):
		self.activator = activator
		self.weights = [0.0 for i in range(input_num)]
		self.bias = 0.0
	
	def __str__(self):
		return 'weights = %s \nbias = %s'%(self.weights,self.bias)
		
	def predict(self,):
		returen self.activator(
		reduce(lambda a,b:a+b,
		,list(map(lambda tp:tp[0]*tp[1],zip(input_vecs,self.weights))),0.0)
		+self.bias)
		
	def 
